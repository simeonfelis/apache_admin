{% if groups %} {% for group in groups %}
{{ group.share.share_type }}_{{ group.share.name|lower|safe }}: {% for user in group.users %}{% if user.is_expired %}{% else %}{{ user.shortName }} {% endif %}{% endfor %}
{% endfor %}{% endif %}
